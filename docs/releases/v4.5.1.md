# V4.5.1 — 本机审核版

尚未推送或发布 GitHub。

- 本机独立 gcli2api 从 6764b89 快进更新至 8e91823，uv sync 完成。
- 上游更新包含客户端版本、OpenAI 工具调用转换、抗截断等修正，并移除 Vertex 路由。本软件使用的 Antigravity / Gemini CLI 路由保留。
- 适配新 `抗截断/` 前缀，与旧 `流式抗截断/`、`假流式/` 一起去重，避免模型列表重复。
- 同档 Gemini 模型按数字版本降序排列，保留 Claude / GPT 的既有优先级。
- 模型动态发现返回 gemini-3.8-flash-high / medium / tiered / low；未伪造可用模型或配额。
- 本机真实调用 gemini-3.8-flash-high 返回 HTTP 200，响应模型一致且正文非空。其他三个变体已发现，未逐个调用。
- 436 项测试通过。版本一致性检查通过。

使用：打开新 EXE → Gemini 反代 → 启动服务/检测服务 → 刷新额度 → 在目标模型旁点击切换使用。

gcli2api 是独立安装，不包含在 EXE 中；本机更新已完成，复制 EXE 到其他电脑不会自动升级该电脑的 gcli2api。
旧上游提交保留为本地分支 backup-before-20260908-6764b89。现有凭证未删除。
